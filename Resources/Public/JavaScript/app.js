// Generated by CoffeeScript 1.12.6
(function() {
  var APP;

  APP = void 0;

  APP = {};

  APP.rwdBackground = function() {
    var backgrounds, backgroundsRaw, changeRwd, el, sizes, sizesPoints, windowWidth;
    el = $('.js-rwd-bg');
    sizes = ['xs', 's', 'm', 'l'];
    sizesPoints = [
      {
        min: 0,
        max: 767
      }, {
        min: 768,
        max: 1023
      }, {
        min: 1024,
        max: 1349
      }, {
        min: 1350,
        max: null
      }
    ];
    backgroundsRaw = $('.js-rwd-bg').attr('sizes');
    backgrounds = $.parseJSON(backgroundsRaw);
    windowWidth = $(window).width();
    changeRwd = function(windowWidth) {
      return $.each(sizesPoints, function(key, value) {
        if (backgrounds[sizes[key]] !== '') {
          if (value.max === null) {
            if (windowWidth > value.min) {
              return el.css("background", "url(" + backgrounds[sizes[key]] + ")");
            } else {

            }
          } else {
            if (windowWidth >= value.min && windowWidth < value.max) {
              return el.css("background", "url(" + backgrounds[sizes[key]] + ")");
            }
          }
        }
      });
    };
    changeRwd(windowWidth);
    return $(window).resize(function() {
      windowWidth = $(window).width();
      return changeRwd(windowWidth);
    });
  };

  APP.init(function() {
    return App.rwdBackground();
  });

  $(document).ready(function() {
    APP.init();
  });

}).call(this);

//# sourceMappingURL=app.js.map
